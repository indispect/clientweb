<%= form_for(@fb_channel) do |f| %>
  <% if @fb_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fb_channel.errors.count, "error") %> prohibited this fb_channel from being saved:</h2>

      <ul>
      <% @fb_channel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
